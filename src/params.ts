export class Params {
    // WORLD
    static readonly chunckSize = 400;
    static readonly bchunckSize = 800;
    static readonly worldSize = 2 * 2 * this.chunckSize; // EVEN NUMBER
    static readonly spawnNoDrawZone = 100;
    static readonly itemLoadBatchSize = 50;
}
